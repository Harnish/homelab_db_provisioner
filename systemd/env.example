# Path to config.json
CONFIG_PATH=/etc/homelab-db-provisioner/config.json

# Set to true to poll config file for changes (required for watch mode service)
WATCH_MODE=true

# Admin UI (optional)
# ADMIN_SITE=true
# ADMIN_USER=admin
# ADMIN_PASSWORD=changeme
# ADMIN_PORT=8080
